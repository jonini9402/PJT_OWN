<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "com.own.domain.bookmark.dao.BookmarkDao">

	<!-- 북마크 -->
	<insert id="insertBookmark">
		INSERT INTO user_post_bookmark (user_id, post_id, created_at)
		VALUES (#{userId}, #{postId}, NOW())
	</insert>
	
	<!--북마크 취소 -->
	<delete id="deleteBookmark">
		DELETE FROM user_post_bookmark
		WHERE user_id = #{userId} AND post_id = #{postId}
	</delete>

	<!-- 특정 포스트의 북마크 개수 세기 -->
	<select id= "countBookmarkByPostId" resultType="int">
		SELECT COUNT(*)
		FROM user_post_bookmark
		WHERE post_id = #{postId}
	</select>
	
	<!-- 특정 유저가 특정 게시물에 북마크했는지 체크 -->
	<select id = "existsBookmark" resultType = "int">
		SELECT 1
		FROM user_post_bookmark
		WHERE post_id = #{postId} AND user_id = #{userId}
		LIMIT 1
	</select>
</mapper>